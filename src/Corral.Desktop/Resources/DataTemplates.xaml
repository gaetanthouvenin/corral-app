<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:converters="clr-namespace:Corral.Desktop.Converters"
  xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Brushes.xaml" />
    <ResourceDictionary Source="Styles.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <!-- Converter Registration -->
  <converters:StringToColorConverter x:Key="StringToColorConverter" />
  <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
  <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

  <!-- Context Menu for Zone Card -->
  <ContextMenu x:Key="ZoneCardContextMenu">
    <MenuItem Header="Delete Zone" Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.DeleteZoneCommand}" CommandParameter="{Binding Id}">
      <MenuItem.Icon>
        <iconPacks:PackIconMaterial Kind="Delete" Width="12" Height="12" Foreground="{StaticResource BrushTextSecondary}" />
      </MenuItem.Icon>
    </MenuItem>
  </ContextMenu>

  <!-- Zone Card Template -->
  <DataTemplate x:Key="ZoneCardTemplate">
    <Border
      Style="{StaticResource CardBorder}"
      Height="220"
      Margin="0,0,24,24"
      CornerRadius="8">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Header (Title + Toggle) -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          
          <TextBlock
            Grid.Column="0"
            Text="{Binding Name}"
            FontSize="16"
            FontWeight="Bold"
            Foreground="{StaticResource BrushTextPrimary}"
            VerticalAlignment="Center"
            TextTrimming="CharacterEllipsis" />

          <!-- Mock Toggle Switch -->
          <Border Grid.Column="1" Width="36" Height="20" Background="{Binding Color, Converter={StaticResource StringToColorConverter}}" CornerRadius="10" VerticalAlignment="Center">
             <Ellipse Width="14" Height="14" Fill="White" HorizontalAlignment="Right" Margin="3" />
          </Border>
        </Grid>

        <!-- Status Indicator -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
          <Ellipse Width="6" Height="6" Fill="{Binding Color, Converter={StaticResource StringToColorConverter}}" VerticalAlignment="Center" />
          <TextBlock Text="ACTIVE" FontSize="10" FontWeight="Bold" Foreground="{StaticResource BrushTextSecondary}" Margin="8,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <!-- Center Icon Placeholder Area -->
        <Border Grid.Row="2" Background="{StaticResource BrushBackground}" CornerRadius="6" Margin="0,0,0,16">
           <iconPacks:PackIconMaterial Kind="ApplicationOutline" Foreground="{Binding Color, Converter={StaticResource StringToColorConverter}}" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>

        <!-- Bottom Actions -->
        <Grid Grid.Row="3">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <Button Grid.Column="0" Command="{Binding EditCommand}" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand">
            <StackPanel Orientation="Horizontal">
              <iconPacks:PackIconMaterial Kind="Pencil" Width="12" Height="12" Foreground="{StaticResource BrushTextSecondary}" VerticalAlignment="Center" Margin="0,0,8,0" />
              <TextBlock Text="EDIT ZONE" FontSize="10" FontWeight="Bold" Foreground="{StaticResource BrushTextSecondary}" VerticalAlignment="Center" />
            </StackPanel>
          </Button>

          <Button Grid.Column="1" Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand" ContextMenu="{StaticResource ZoneCardContextMenu}">
            <iconPacks:PackIconMaterial Kind="DotsHorizontal" Width="14" Height="14" Foreground="{StaticResource BrushTextSecondary}" VerticalAlignment="Center" />
          </Button>
        </Grid>

      </Grid>
    </Border>
  </DataTemplate>

</ResourceDictionary>
